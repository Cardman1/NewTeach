<Page x:Class="TeachBook.View.Pages.MenuPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TeachBook.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MenuPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Column="1">
            <Button Margin="10" Click="AddStydent">Добавить студента</Button>
            <Button Margin="10" Click="AddOtchenku">Добавить оценку</Button>
            <Button Margin="10" Click="AllStudent">Список студентов</Button>
            <Button Margin="10" Click="Redact">Редактировать оценки</Button>
            <Button Margin="10" Click="Delete">Удалить студента</Button>
        </StackPanel>
        
    </Grid>
</Page>
